{
  "name": "VoteApp-Aggregator",
  "version": "0.0.0",
  "description": "VoteApp Demo using API Gateway and Lambda. This function aggregates votes from one DynamoDB into a second.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jeffnunn@amazon.com",
  "dependencies": {
    "aws-sdk": ">= 2.1.25"
  }
}
